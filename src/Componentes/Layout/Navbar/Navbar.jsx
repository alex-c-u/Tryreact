import styles from './Navbar.module.css'

function Navbar() {
    return (
        <Navbar className={styles.main}>
            <h1>todo lo que viene aca es el navbar</h1>
        </Navbar>
    );
}

export default Navbar;